services:
  mimo2api-fix:
    build: .
    container_name: mimo2api-fix
    restart: unless-stopped
    ports:
      - "4003:4003"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    environment:
      - PORT=4003
      - NODE_ENV=production
